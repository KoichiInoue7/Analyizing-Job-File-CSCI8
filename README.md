This is another project I’ve built for my data science class. The goal of this project was to filter out a given dataset and 
to perform SQL functions to manipulate the data further. Throughout this project, I was balancing the task of learning the Python 
language and learning to implement SQL functions. This project taught me the syntax for creating basic functions for elementary operations,
but more importantly, made me think about how each SQL function operates. I compared how arrays worked differently in Python compared to C++, 
as basic manipulation, such as adding to each element, didn’t require loops. This made me think about how the SQL function .column turns tables 
into arrays for operations to be applied, which was different than .where(), which outputs a table and couldn’t be manipulated. I gained 
familiarity with np. to do operations on arrays and are. to sort specific parameters. To further my knowledge, I want to understand how 
things like regression lines on graphs are created with machine learning. How to program that tells the computer to take in data to make predictions.

